version: 2
build:
  os: "ubuntu-20.04"
  tools:
    python: "3.10"
sphinx:
  configuration: docs/source/conf.py  # Ensure this path is correct
formats: all

python:
  version: 3.10  # Just specify the major and minor version, e.g., 3.12
  install:
    - method: pip
      requirements: project_explainer_ui/requirements.txt
