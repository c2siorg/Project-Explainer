version: 2
build:
  os: "ubuntu-20.04"  # Specifies the build OS. Use only if you have specific requirements.
  tools:
    python: "3.12.2"
sphinx:
  configuration: docs/source/conf.py
formats: all
python:
  version: 3.12.2
  install:
    - method: pip
    - requirements: project_explainer_ui/requirements.txt
