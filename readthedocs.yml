version: 2

build:
  os: "ubuntu-20.04"
  tools:
    python: "latest"
sphinx:
  configuration: docs/source/conf.py  # Ensure this path is correct
formats: all

python:
  version: latest  # Just specify the major and minor version, e.g., 3.12
  install:
    - method: pip
      requirements: project_explainer_ui/requirements.txt
